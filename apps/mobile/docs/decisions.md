# 設計決定ログ（Decision Log）

> ルール：決めたことはここに1〜3行で残す。後でブレないため。

## 2026-01-27
- バックエンド無し。学習は端末内SQLiteでstatsカウント更新方式。
- 店提案はMVPはマップディープリンク（APIキー不要）で実装。
- 推論は P(answer|genre,question) をカウント+スムージングで算出し、logスコアで更新する。
